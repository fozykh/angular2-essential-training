import { Component } from '@angular/core';

// For a component we will need at least 2 attr - Selector and template/ template URL
@Component({
  selector: 'app',
  template: '<h1> My App </h1>',
})
export class AppComponent {
  
}